# Tell Lombok that this is your root directory and that it should not search parent directories for more configuration files
#config.stopBubbling=true

# Prevent auto-generated code from being included in test coverage stats
lombok.addLombokGeneratedAnnotation=true

# Throw IllegalArgumentException instead of the default (NullPointerException) when @NonNull not satisfied
lombok.nonNull.exceptionType=IllegalArgumentException